## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`

This was generated via Google AI studio which by default asks for your Gemini API key but this doesn't use any AI features so you should be fine without it.
